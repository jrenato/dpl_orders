# Django Secret Key
# $ python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=your-secret-key

# Debug Mode
DJANGO_DEBUG=True

DJANGO_STATIC_ROOT=/path/to/collect/static/files
DJANGO_MEDIA_ROOT=/path/to/store/media/files

# Path to import sheets. Expected subdirectories:
# * Automático
# * Pedidos Antigos
# * Pedidos
# * Produtos
IMPORT_PATH=/path/to/import/sheets

# Database Configuration
DB_NAME=your-database-name
DB_USER=your-database-user
DB_PASSWORD=your-database-password
DB_HOST=your-database-host
DB_PORT=5432

# Vialogos Database Configuration
VL_INTEGRATION=False
VL_DB_NAME=vldados
VL_DB_USER=your-database-user
VL_DB_PASSWORD=your-database-password
VL_DB_HOST=localhost
VL_DB_PORT=1433
VL_DB_ODBC_DRIVER=ODBC Driver 17 for SQL Server

# Email Configuration (SMTP)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-email-password
EMAIL_USE_TLS=True

# Metabooks
MB_URL=http://www.metabooks.com/api/v2
MB_USERNAME=username
MB_PASSWORD=password
